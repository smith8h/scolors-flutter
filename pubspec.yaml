name: scolors
description: A colors playful dependency.
version: 1.0.0

repository:
  type: git
  url: https://github.com/smith8h/scolors-flutter


environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
